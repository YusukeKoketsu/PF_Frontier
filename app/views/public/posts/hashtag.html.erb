<h2>ハッシュタグ #<%= @tag.hashname %></h2>
  <ul>
    <% @posts.each do |post| %>
      <li>
        <%= link_to post_path(post.id) do %>
        <%= image_tag post.get_image("600x600") %>
        <% end %>
      </li>
    <% end %>
  </ul>